@model IEnumerable<Domain.Dashboards.Item>
@{
    ViewBag.Title = "_ItemsPartial";
}

<div class="row">
    @foreach (var item in Model)
    {
        @Html.Action("_ItemPartial", new { grafiekType = 0, index = 0, labels = ViewBag.Labels, data = ViewBag.Data, page = "dashboard" })
    }
    <div class="card mb-3"><div class="card-body">test</div></div>

</div>

